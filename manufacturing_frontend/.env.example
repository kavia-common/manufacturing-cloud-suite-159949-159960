# Base URL for backend API (without trailing slash)
# In dev, vite.config.js proxies /api to this target. Example for FastAPI running locally:
VITE_API_BASE_URL=http://localhost:8000

# Optional: WebSocket base if different
# VITE_WS_BASE_URL=ws://localhost:8000
