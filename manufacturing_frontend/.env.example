# Copy to .env and adjust as needed for your environment.

# Base URL for backend API used by Vite dev proxy (e.g., http://localhost:8000)
VITE_API_BASE_URL=http://localhost:8000

# Base URL for WebSocket endpoints. If not set, falls back to VITE_API_BASE_URL
VITE_WS_BASE_URL=ws://localhost:8000
